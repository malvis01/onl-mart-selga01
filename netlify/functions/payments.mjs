const headers={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"Content-Type, Authorization","Access-Control-Allow-Methods":"POST, OPTIONS"};
const json=(b,s=410)=>new Response(JSON.stringify(b),{status:s,headers:{...headers,"Content-Type":"application/json"}});
export default async function handler(req){if(req.method==="OPTIONS")return new Response("ok",{headers});return json({error:"This payment endpoint has been retired. Please use the SALGA order checkout flow: provide delivery details, wait for the seller to set the delivery fee, then complete secure payment."},410);}
export const config={path:"/api/payments"};