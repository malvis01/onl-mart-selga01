import { createClient } from "@supabase/supabase-js";
const SUPABASE_URL=process.env.SUPABASE_URL||process.env.VITE_SUPABASE_URL;const SUPABASE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.VITE_SUPABASE_PUBLISHABLE_KEY||process.env.SUPABASE_ANON_KEY||process.env.VITE_SUPABASE_ANON_KEY;const supabase=createClient(SUPABASE_URL,SUPABASE_KEY);
const headers={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"Content-Type, Authorization","Access-Control-Allow-Methods":"GET, POST, OPTIONS","Content-Type":"application/json"};const json=(b,s=200)=>new Response(JSON.stringify(b),{status:s,headers});
async function getUser(req){const a=req.headers.get("authorization");if(!a?.startsWith("Bearer "))return null;const {data,error}=await supabase.auth.getUser(a.slice(7));return error||!data?.user?null:data.user;}
async function getOrders(req){const user=await getUser(req);if(!user)return json({error:"Login required"},401);const {data:profile}=await supabase.from("profiles").select("id,role,phone,full_name").eq("id",user.id).maybeSingle();if(!profile)return json({error:"User profile not found"},404);if(profile.role==="buyer"){const {data,error}=await supabase.from("orders").select(`id,buyer_id,business_id,total_amount,product_subtotal,delivery_fee,service_fee,status,payment_status,delivery_address,buyer_phone,notes,created_at,updated_at,businesses(id,business_name,logo_url),order_items(id,product_id,quantity,unit_price,products(id,name,image_url))`).eq("buyer_id",user.id).order("created_at",{ascending:false});if(error)return json({error:error.message},500);return json({orders:data||[]});}if(profile.role==="seller"||profile.role==="business_owner"){const {data:businesses,error:be}=await supabase.from("businesses").select("id").eq("owner_id",user.id);if(be)return json({error:be.message},500);const ids=(businesses||[]).map(b=>b.id);if(!ids.length)return json({orders:[]});const {data,error}=await supabase.from("orders").select(`id,buyer_id,business_id,total_amount,product_subtotal,delivery_fee,service_fee,buyer_phone,delivery_address,status,payment_status,notes,created_at,updated_at,businesses(id,business_name,logo_url),order_items(id,product_id,quantity,unit_price,products(id,name,image_url))`).in("business_id",ids).order("created_at",{ascending:false});if(error)return json({error:error.message},500);return json({orders:data||[]});}return json({error:"Unsupported account role"},403);}
export default async req=>{try{if(req.method==="OPTIONS")return new Response("ok",{headers});if(req.method==="GET")return await getOrders(req);if(req.method==="POST")return json({error:"Legacy order creation is retired. Use the SALGA Buy flow: delivery details first, seller delivery fee second, secure payment third."},410);return json({error:"Method not allowed",allowed_methods:["GET","OPTIONS"]},405);}catch(e){console.error("ORDERS FUNCTION ERROR",e);return json({error:e?.message||"Unable to process order."},500)}};
export const config={path:"/api/orders"};