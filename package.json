{
  "name": "salga-digital-mart-production",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp netlify.toml dist/netlify.toml"
  },
  "dependencies": {
    "@netlify/blobs": "^9.0.0",
    "@netlify/functions": "^3.1.0",
    "@supabase/supabase-js": "^2.57.0",
    "openai": "^4.104.0"
  }
}
